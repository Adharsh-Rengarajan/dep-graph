# dep-graph
Dependency graph for Composio's tool catalog (1,248 tools across Google Workspace + GitHub). Classifies every input parameter as user / tool / static, links each tool-sourced param to its producer. Heuristic extractor + interactive viewer. ~250ms, deterministic, no LLM.
